name: Reusable Create/Update PR

on:
  workflow_call:
    inputs:
      skip-tests:
        type: boolean
        required: false
        default: false

      min-coverage:
        type: number
        required: false
        default: 80

      coverage-mode:
        type: string
        required: false
        default: "informative"    # informative | blocking

      # coverage-command:
      #   type: string
      #   required: false
      #   default: ""

      # coverage-file:
      #   type: string
      #   required: false
      #   default: "coverage/coverage-summary.json"

    secrets:
      GH_TOKEN:
        required: true

jobs:
  check-branch:
    runs-on: ubuntu-latest
    outputs:
      skip: ${{ steps.branch.outputs.skip }}

    # permissions:
    #   contents: read
    #   pull-requests: write

    steps:
      - name: 📌 Resolve PR base branch
        id: branch
        run: |
          HEAD="${{ github.event.workflow_run.head_branch }}"
          echo "Detected head branch: $HEAD"

          if [ "$HEAD" = "main" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          elif [ "$HEAD" = "develop" ]; then
            echo "base=main" >> $GITHUB_OUTPUT
          else
            echo "base=develop" >> $GITHUB_OUTPUT
          fi

          echo "head=$HEAD" >> $GITHUB_OUTPUT

      - name: 🚫 Skip main branch
        if: steps.branch.outputs.skip == 'true'
        run: echo "Main branch detected. Skipping auto PR."

  run-coverage:
    needs: check-branch
    if: needs.check-branch.outputs.skip != 'true' && inputs.skip-tests != true 
    runs-on: ubuntu-latest
    outputs:
      line: ${{ steps.coverage.outputs.line }}
      status: ${{ steps.coverage-check.outputs.status }}

    steps:
      - name: 📥 Download coverage artifact
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
          path: coverage

      - name: 📊 Read coverage report
        id: coverage
        run: |
          FILE="coverage/coverage-summary.normalized.json"

          if [ ! -f "$FILE" ]; then
            echo "⚠️ Coverage artifact not found"
            echo "line=" >> $GITHUB_OUTPUT
            exit 0
          fi

          LINE=$(jq '.line' "$FILE")
          echo "line=$LINE" >> $GITHUB_OUTPUT
       

      - name: ✅ Evaluate coverage
        id: coverage-check
        run: |
          LINE=${{ steps.coverage.outputs.line }}
          MIN=${{ inputs.min-coverage }}
          MODE="${{ inputs.coverage-mode }}"

          STATUS="passed"

          if [ -n "$LINE" ] && (( $(echo "$LINE < $MIN" | bc -l) )); then
            STATUS="failed"
            if [ "$MODE" = "blocking" ]; then
              echo "Coverage below threshold"
              exit 1
            fi
          fi

          echo "status=$STATUS" >> $GITHUB_OUTPUT


  create-pr:
    needs:
      - check-branch
      - run-coverage
    if: needs.check-branch.outputs.skip != 'true'
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: ⬇️ Checkout consumer repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0    # Necessário para evitar falhas do gh CLI em repositórios grandes

      - name: 🔐 Authenticate GitHub CLI
        run: |
          # echo "${GITHUB_TOKEN}" | gh auth login --with-token
          gh auth setup-git
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: 🏷 Ensure auto-generated label exists
        run: |
          gh label create auto-generated --description "Pull Request criada automaticamente" --color 006B75 --force 
          # gh label create "pending-review" --description "Aguardando revisão" --color "FBCA04" || true
          # gh label create "coverage-failed" --description "Cobertura de testes abaixo do limite mínimo." --color "D93F0B" || true
          # gh label create "coverage-passed" --description "Cobertura de testes acima do limite mínimo." --color "0E8A16" || true
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: 📝 Generate PR body
        run: |
          COVERAGE_LINE="${{ needs.run-coverage.outputs.line }}"
          COVERAGE_STATUS="${{ needs.run-coverage.outputs.status }}"
          MIN_COVERAGE="${{ inputs.min-coverage }}"
          MODE="${{ inputs.coverage-mode }}"

          if [ -n "$COVERAGE_LINE" ]; then
            COVERAGE_BLOCK=$(cat <<EOF
          ### 📊 Cobertura de Testes

          | Métrica | Valor |
          |--------|-------|
          | Linhas | **${COVERAGE_LINE}%** |
          | Mínimo exigido | ${MIN_COVERAGE}% |
          | Status | ${COVERAGE_STATUS} |
          | Modo | ${MODE} |

          EOF
          )
          else
            COVERAGE_BLOCK=$(cat <<EOF
          ### 📊 Cobertura de Testes

          ⚠️ Cobertura não gerada para este projeto.
          EOF
          )
          fi

          cat <<EOF > pr_body.md
          ## 🤖 Pull Request Automática

          Esta PR foi criada ou atualizada automaticamente.

          **Origem:** \`${{ steps.branch.outputs.head }}\`  
          **Destino:** \`${{ steps.branch.outputs.base }}\`

          ---
          ✔ CI executado com sucesso  
          ✔ Testes validados  
          ✔ Processo automatizado

          ${COVERAGE_BLOCK}

          ---
          _Gerado automaticamente pela action **auto-pr**_
          EOF

      - name: 📜 Create or Update Pull Request
        run: |
          BASE="${{ steps.branch.outputs.base }}"
          HEAD="${{ steps.branch.outputs.head }}"
          TITLE="Auto PR: ${HEAD} → ${BASE}"

          # Tenta encontrar um PR existente
          PR_NUMBER=$(gh pr list --base "$BASE" --head "$HEAD" --json number --jq '.[0].number')

          echo "👉 $TITLE"

          if [ -z "$PR_NUMBER" ]; then
            echo "No existing PR found. Creating new PR..."
            gh pr create \
              --base "$BASE" \
              --head "$HEAD" \
              --title "$TITLE" \
              --body-file pr_body.md \
              --label auto-generated \
              --assignee "${{ github.actor }}"
          else
            echo "Existing PR #$PR_NUMBER found. Updating..."
            gh pr edit "$PR_NUMBER" \
              --title "$TITLE" \
              --body-file pr_body.md \
              --add-assignee "${{ github.actor }}"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
