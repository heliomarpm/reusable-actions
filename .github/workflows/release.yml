name: Reusable Release

on:
  workflow_call:
    inputs:
      project_path:
        required: false
        type: string
        default: "."
      stack:
        description: 'Stack the project. (node | php | dotnet | python | go | empty for auto-detect)'
        required: false
        type: string
        default: null     # node | php | dotnet | python | go | empty for auto-detect
      semantic_release_config:
        description: 'Path to semantic-release config file.'
        required: false
        type: string
      semantic_release_dry_run:
        description: 'Dry run mode for semantic-release. (default: false -> true for PRs)'
        required: false
        type: boolean
      semantic_release_debug_mode:
        description: 'Debug mode for semantic-release. (default: false)'
        required: false
        type: boolean

    secrets:
      GH_TOKEN:
        required: true

jobs:
  semantic-release:
    runs-on: ubuntu-latest
    env:
      REUSABLE_PATH: __reusable_actions__

    defaults:
      run:
        shell: bash
        working-directory: ${{ inputs.project_path }}

    permissions:
      actions: read
      contents: write
      issues: write
      # pull-requests: write

    steps:
      - name: Checkout consumer repository
        uses: actions/checkout@v4
        with: 
          # A linha 'ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0 	  # Necessário para o semantic-release inspecionar histórico
          fetch-tags: true 	# Necessário para o semantic-release inspecionar tags
          persist-credentials: true  # → permite push com GITHUB_TOKEN
          token: ${{ secrets.GH_TOKEN }} # Necessário para autenticar PUSH

      - name: Checkout reusable-actions scripts
        uses: actions/checkout@v4
        with:
          repository: heliomarpm/reusable-actions
          ref: main
          path: ${{ env.REUSABLE_PATH }}
          token: ${{ secrets.GH_TOKEN }} # Necessário se o repositório for privado

      - name: Files in workspace
        run: |
          echo "📂 Current directory:"
          pwd
          echo "📄 Files in root:"
          ls -la

      - name: ⚙️ Configurar Usuário Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 🔍 Resolve project stack
        id: stack
        working-directory: ${{ inputs.project_path }}
        run: |
          if [[ -n "${{ inputs.stack }}" ]]; then
            echo "Using stack from input: ${{ inputs.stack }}"
            echo "value=${{ inputs.stack }}" >> $GITHUB_OUTPUT
            exit 0
          fi

          chmod +x $REUSABLE_PATH/scripts/shared/detect-stack.sh
          STACK=$($REUSABLE_PATH/scripts/shared/detect-stack.sh)

          if [[ -z "$STACK" ]]; then
            echo "value=" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "✅ Detected stack: $STACK"
          echo "value=$STACK" >> $GITHUB_OUTPUT

      # - name: 🔍 Detect release config
      #   id: releaserc
      #   run: |
      #     chmod +x $REUSABLE_PATH/scripts/shared/detect-releaserc.sh
      #     HAS_CONFIG=$($REUSABLE_PATH/scripts/shared/detect-releaserc.sh)
      #     echo "has_config=$HAS_CONFIG" >> $GITHUB_OUTPUT

      - name: 🚀 Run Release
        if: steps.stack.outputs.value != ''
        run: |
          SCRIPT="$REUSABLE_PATH/scripts/shared/semantic-release.sh"
          chmod +x "$SCRIPT"
          "$SCRIPT"
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          STACK: ${{ steps.stack.outputs.value }}
          REUSABLE_PATH: ${{ env.REUSABLE_PATH }}
          SEMANTIC_RELEASE_CONFIG: ${{ inputs.semantic_release_config }}
          SEMANTIC_RELEASE_DRY_RUN: ${{ inputs.semantic_release_dry_run }}
          SEMANTIC_RELEASE_DEBUG_MODE: ${{ inputs.semantic_release_debug_mode }}

      - name: Fail if stack not detected
        if: steps.stack.outputs.value == ''
        run: |
          echo "❌ Stack not detected. Check project_path or repository structure."
          exit 1

